<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- V8 compiles JavaScript to machine code at runtime; the hardened runtime
       blocks that unless we say so. All three are standard for Electron. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- Stash-specific: auto-paste tells System Events to press Cmd+V in whatever
       app was focused. Driving another app is Apple Events, which the hardened
       runtime blocks without this. Without it auto-paste fails silently. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
</dict>
</plist>
